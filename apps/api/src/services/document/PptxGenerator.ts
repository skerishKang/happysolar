// @ts-ignore
const PptxGenJS = require('pptxgenjs');
import { Document } from "./types";

export async function generatePPTX(document: Document): Promise<Buffer> {
  // PPTX 생성 로직 (실제 구현은 storage.ts에서 복사/이동 필요)
  return Buffer.from([]);
} 